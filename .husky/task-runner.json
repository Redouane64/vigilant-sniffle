{
   "tasks": [
      {
         "name": "dotnet-format",
         "command": "dotnet",
         "args": [ "dotnet-format", "--include", "${staged}", "-v", "m" ],
         "include": [ "**/*.cs", "**/*.vb" ]
      }
   ]
}
